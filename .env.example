OPENAI_API_KEY=your_key_here

# Optional: Postgres (defaults match docker-compose)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=agentdb
# POSTGRES_USER=n8n
# POSTGRES_PASSWORD=n8npassword

# Optional: SMTP / MailHog
# SMTP_HOST=localhost
# SMTP_PORT=1025
# MAILER_FROM=reports@local.demo

# Optional: Agent URLs (when not default)
# REPORT_AGENT_URL=http://localhost:8000
# DB_AGENT_URL=http://localhost:8001/mcp
# MAILER_AGENT_URL=http://localhost:8002/mcp

# Optional: Use n8n as ReportAgent backend (no DbAgent/MailerAgent or OPENAI needed for ReportAgent)
N8N_WEBHOOK_URL=http://localhost:5678/webhook/db-email-report
